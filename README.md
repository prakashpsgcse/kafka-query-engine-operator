# kafka-query-engine-operator
kafka-query-engine-operator is a Kubernetes Operator that manages the full lifecycle of search indexers built on Apache Kafka and Apache Lucene. It creates and maintains a Spring Boot API control plane, and automatically provisions the necessary ConfigMaps, Deployments, and Services, enabling a powerful text search functionality over Kafka topics.
